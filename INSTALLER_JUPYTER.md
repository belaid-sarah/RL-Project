# 📦 Installation de Jupyter Notebook

## Installation Rapide

```bash
pip install jupyter notebook
```

## Vérifier l'Installation

```bash
jupyter --version
```

## Lancer Jupyter Notebook

```bash
jupyter notebook
```

Cela ouvre automatiquement votre navigateur avec l'interface Jupyter.

## Ouvrir le Rapport

Une fois Jupyter lancé :
1. Cliquez sur `rapport_projet_rl.ipynb`
2. Le notebook s'ouvre dans un nouvel onglet
3. Exécutez les cellules avec `Shift + Enter` ou `Cell` → `Run All`

## Installer les Dépendances pour le Notebook

Le notebook utilise pandas, matplotlib, etc. Installer avec :

```bash
pip install pandas matplotlib numpy seaborn
```

Ou si vous avez un fichier requirements.txt :

```bash
pip install -r requirements.txt
```

